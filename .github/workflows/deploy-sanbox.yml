name: Deploy to Sandbox

on:
  push:
    branches: ["developpement"]
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    # si tes secrets sont dans un Environment, dé-commente:
    # environment: sandbox

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Clé SSH depuis secret: préfère SSH_PRIVATE_KEY_B64, sinon SSH_PRIVATE_KEY (plain OpenSSH)
      - name: Write SSH key (B64 or plain)
        shell: bash
        run: |
          mkdir -p ~/.ssh
          if [ -n "${{ secrets.SSH_PRIVATE_KEY_B64 }}" ]; then
            echo "${{ secrets.SSH_PRIVATE_KEY_B64 }}" | base64 -d > ~/.ssh/id_ci
            echo "Loaded key from SSH_PRIVATE_KEY_B64"
          elif [ -n "${{ secrets.SSH_PRIVATE_KEY }}" ]; then
            printf "%s\n" "${{ secrets.SSH_PRIVATE_KEY }}" | sed 's/\r$//' > ~/.ssh/id_ci
            echo "Loaded key from SSH_PRIVATE_KEY"
          else
            echo "::error::Neither SSH_PRIVATE_KEY_B64 nor SSH_PRIVATE_KEY is set."
            exit 1
          fi
          chmod 600 ~/.ssh/id_ci
          # Vérifie le format (doit afficher une empreinte)
          ssh-keygen -yf ~/.ssh/id_ci | ssh-keygen -lf -

      - name: SSH sanity
        shell: bash
        run: |
          ssh -i ~/.ssh/id_ci -p ${{ secrets.SSH_PORT }} -o StrictHostKeyChecking=no \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "echo OK-from-CI"

      - name: Create remote deploy script
        shell: bash
        env:
          APP: ${{ secrets.SANDBOX_PATH }}
        run: |
          cat > deploy-sandbox.sh <<'SH'
          set -euo pipefail

          APP="__APP__"
          PHP="/opt/alt/php82/usr/bin/php"
          COMPOSER="$PHP /usr/bin/composer"

          echo "[1/6] Code → ${APP}"
          cd "$APP"
          if [ -d .git ]; then
            git fetch origin --prune
            git reset --hard origin/developpement
          else
            echo "⚠️  Pas de dépôt Git dans $APP (skip fetch/reset)"
          fi

          echo "[2/6] Composer install"
          $COMPOSER install --no-interaction --prefer-dist

          echo "[3/6] Node 20 via nvm + npm install/build"
          export NVM_DIR="$HOME/.nvm"
          if [ ! -s "$NVM_DIR/nvm.sh" ]; then
            curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
          fi
          . "$NVM_DIR/nvm.sh"
          nvm install 20
          nvm use 20
          if [ -f package-lock.json ]; then npm ci; elif [ -f package.json ]; then npm install; fi
          if [ -f package.json ]; then npm run build; fi

          echo "[4/6] Doctrine migrations (dev)"
          $PHP bin/console doctrine:migrations:migrate --no-interaction --env=dev

          echo "[5/6] Cache clear + warmup (dev)"
          $PHP bin/console cache:clear --env=dev
          $PHP bin/console cache:warmup --env=dev

          echo "[6/6] Done."
          SH
          sed -i "s|__APP__|$APP|g" deploy-sandbox.sh
          chmod +x deploy-sandbox.sh

      - name: Upload deploy script
        shell: bash
        run: |
          scp -i ~/.ssh/id_ci -P ${{ secrets.SSH_PORT }} -o StrictHostKeyChecking=no \
            ./deploy-sandbox.sh \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/tmp/deploy-sandbox.sh

      - name: Run remote deploy script
        shell: bash
        run: |
          ssh -i ~/.ssh/id_ci -p ${{ secrets.SSH_PORT }} -o StrictHostKeyChecking=no \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "bash /tmp/deploy-sandbox.sh && rm -f /tmp/deploy-sandbox.sh"
